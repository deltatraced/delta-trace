
# 1 Problem

`*.agda-lib` does not alert us that `--cubical` is not supported with compile, but on CLI we do get a warning.

# 2 Notes

Spawn [[000 Notes for agda-lib incl cubical should warn]] ^spawn-entry-114704